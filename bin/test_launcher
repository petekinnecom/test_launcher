#!/usr/bin/env ruby

require "test_launcher/cli/input_parser"
require "test_launcher"

input = InputParser.new(ARGV)
TestLauncher.launch(input.query, run_all: input.options[:run_all], framework: input.options[:framework])
